import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    // drawer: null,
  },
  mutations: {
    // drawer(state) {
    //   state.drawer = !state.drawer
    // }
  },
  actions: {
    // drawer(context) {
    //   context.commit('drawer')
    // }
  },
  modules: {
  }
})
